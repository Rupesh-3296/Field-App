name: Run Bash Script

on: [push]

jobs:
  run-bash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run bash commands
        run: |
          echo "Hello from GitHub Bash"
          ls -la
          pwd
